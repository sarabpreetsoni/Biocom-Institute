rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to upload and view assignment materials
    match /assignments/{allPaths=**} {
      allow read: if true; // Or restrict to 'request.auth != null'
      allow write: if request.auth != null;
    }
  }
}
